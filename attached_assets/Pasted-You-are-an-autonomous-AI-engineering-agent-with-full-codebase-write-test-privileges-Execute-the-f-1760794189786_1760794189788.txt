You are an autonomous AI engineering agent with full codebase write & test privileges. Execute the following remediation tasks in the exact order given and do not skip any item. For each change: create a clear Git commit (one commit per logical unit or file group), run type checks and tests, and attach a short commit message. If any automated test or build fails, stop, record the failure, produce a concise error summary and remediation plan, then continue with the next item only after the failure is fixed. After all tasks succeed, produce a single summary report with changed files, commits, test results, and deployment steps.

BEFORE START

Create a new branch remediation/full-p0-p1-p2 from main.

Install dependencies: npm ci.

Ensure Node >= 22.20.0 and npm >= 11.6.2 are used.

STEP A — GLOBAL PROJECT & CONFIG CLEANUP
A.1 Remove Replit: remove any Replit-specific files/configs (e.g. .replit, replit.nix, replit-specific plugins in package.json, replit-specific scripts). Commit: chore: remove replit configs.
A.2 Add/Update tooling:

Add .env.example with all required env keys and safe defaults (include descriptions).

Add postcss.config.js, .prettierrc, .editorconfig, .husky pre-commit hooks and lint-staged to run eslint --fix and prettier --write.

Ensure package.json scripts include ci:check (typecheck + lint + test) and ci:build. Commit: chore: add developer tooling and examples.

STEP B — DATABASE & INFRASTRUCTURE (P0 CRITICAL)
B.1 Database connection pool hanging (CRIT-001)

Inspect server/db.ts. Add robust connection diagnostics and timeouts.

If using Neon WebSocket, implement fallback to TCP or configure keepAlive/connectionTimeoutMillis and maxRetries. Add exponential backoff on connect and circuit breaker wrapper around DB queries.

Add Prometheus metrics for pool_active, pool_idle, pool_waiting.

Add unit test to reproduce query hanging (mock) and prove fallback works.

Commit: fix(db): stabilize connection pool, add diagnostics & metrics.

B.2 Add missing indexes (CRIT-002)

Update shared/schema.ts migrations: add indexes for users(email), users(username), products(category), products(websiteId), orders(userId), orders(status), posts(slug), posts(websiteId), sessions(userId), sessions(expiresAt).

Create a new migration file using drizzle-kit: migrations/XXXX_add_indexes.sql (or drizzle migration equivalent).

Run drizzle-kit push (or show migration SQL) and include SQL CREATE INDEX CONCURRENTLY where supported.

Commit: fix(db): add missing indexes for high-read fields.

B.3 Sanitize DB connection string in logs (CRIT-011)

Implement sanitizeConnectionString(url) and ensure any errors logged use sanitized string. Replace all logger.error(DATABASE_URL) or similar with sanitized output.

Commit: fix(security): sanitize DB URL in logs.

B.4 Circuit breaker persistence (CRIT-012)

Implement persistence of circuit breaker state in Redis (or DB) in server/db.ts. On state change, persist; on startup, restore.

Add tests validating restart preserves state.

Commit: fix(db): persist circuit breaker state to redis.

STEP C — TYPECHECK & DEPENDENCIES (CRIT-003)
C.1 Fix TypeScript compilation errors (all 36)

Install any missing types and runtime deps: @sentry/node, nodemailer, @google-cloud/storage only if used—otherwise remove imports and update code to optional load.

Resolve schema mismatches: sync TypeScript models to shared/schema.ts and update Zod schemas to match DB columns. Where DB changed (indexes/columns), update TS types.

Implement missing methods (e.g., TransformersProvider), fix updateOrder storage method signatures, and add explicit types to remove implicit any.

Run tsc --noEmit and fix all errors until clean.

Commit: fix(types): resolve TS errors and sync schema types.

STEP D — ENV VALIDATION (CRIT-004)
D.1 Harden server/env.validation.ts

Add explicit validations:

TWO_FACTOR_BACKUP_ENCRYPTION_KEY: require length >= 32 bytes (or configured length); use z.string().min(32).

WEBHOOK_SIGNATURE_SECRET: require presence and min length.

FILE_ENCRYPTION_KEY: require length and base64 format.

Validate email provider ENV variables (SMTP host, port, username, encrypted password).

Add integration test that fails startup when invalid env provided.

Commit: fix(env): add strict env validations.

STEP E — SESSIONS & RACE CONDITIONS (CRIT-005)
E.1 Fix createSessionWithLimit race

Use a transaction or Lua script (if Redis used) to atomically enforce session limit per user:

If session store is DB: wrap in transaction with SELECT FOR UPDATE on user's sessions or use row lock to count sessions and insert.

If session store in Redis: use Lua script to check length and insert atomically.

Add unit test simulating concurrent session creation and assert max sessions <= configured limit (default 5).

Commit: fix(session): atomic session creation with limit.

STEP F — CSRF BOOTSTRAP (CRIT-006)
F.1 Fix client bootstrap logic

Update client/src/lib/csrf-manager.ts to expose a Promise-based bootstrap and add a max retry count and total timeout (e.g., 5 attempts, exponential backoff, 30s total).

Update client/src/App.tsx to await bootstrap before rendering critical components or show a controlled loading state and fail gracefully with error UI.

Add tests to ensure no infinite loops.

Commit: fix(csrf): robust bootstrap with bounded retries.

STEP G — WEBSOCKET & REAL-TIME (CRIT-007 & HIGH-007)
G.1 WebSocket room cleanup & revalidation

Update server/websocket.ts:

Ensure room cleanup runs when last socket disconnects and add periodic GC task (e.g., every 5 minutes) to sweep empty rooms.

Replace WeakSet misuse with Map metadata and timeouts to ensure deterministic cleanup.

Implement token re-validation every 15 minutes on long-lived sockets: schedule refresh check and close connection if token expired.

Add tests for simultaneous disconnects and token expiry behavior.

Commit: fix(ws): deterministic room cleanup & token revalidation.

STEP H — FILE UPLOAD & TRANSACTIONS (CRIT-008)
H.1 Make upload atomic

Change upload flow in server/routes.ts to:

Stream upload to temporary location.

Insert DB record within transaction.

Move file from temp to final location only after DB commit; if DB insert fails, delete temp file.

If using object storage (S3): upload to a temp prefix and copy/commit on DB success; on failure delete object.

Add background cleanup cron for orphaned files older than X minutes.

Add tests simulating DB insert failure and verifying no orphan files remain.

Commit: fix(upload): atomic file save with db transaction and cleanup.

STEP I — STRIPE WEBHOOK SECURITY (CRIT-009)
I.1 Use timing-safe signature validation

Replace naive string compare with crypto.timingSafeEqual() using buffers. Also verify signature timestamps and reject outside tolerance (e.g., 5 minutes).

Validate webhook secret existence and fail early.

Add unit tests for signature validation, timing safety, and timestamp tolerance.

Commit: fix(security): timing-safe stripe webhook signature validation.

STEP J — PASSWORD RESET TOKEN (CRIT-010)
J.1 Invalidate tokens on first use

Update server/auth-enhanced.ts:

Mark tokens as consumedAt upon successful use within a transaction.

Reject reuse by checking consumed timestamp.

Optionally rotate/reset token value after successful reset.

Add audit log entry for token usage.

Commit: fix(auth): consume password reset tokens to prevent reuse.

STEP K — LOGGING, SECURITY HARDENING, & SQL SAFETY (HIGH items)
K.1 Replace vulnerable crypto usage (HIGH-001)

Replace crypto.randomBytes(32) usages for token generation with crypto.randomUUID() or use Node webcrypto.getRandomValues when appropriate; document where deterministic length bytes are required.

Commit: fix(security): use modern secure randomness APIs.

K.2 Improve session fingerprint entropy (HIGH-002)

Add Accept-Language, a hashed screen/device fingerprint, and a HMAC secret when computing fingerprint; ensure fingerprint is stable but high-entropy.

Commit: fix(session): strengthen fingerprint entropy.

K.3 Rate limit 2FA backup code verification (HIGH-003)

Add rate limiter middleware to the 2FA verification endpoint with limit 3 attempts/hour per user and per IP.

Commit: fix(security): rate limit 2FA backup code attempts.

K.4 Hash API keys (HIGH-004)

If API keys stored in DB, update shared/schema.ts to include api_keys.hash and migrate existing keys by hashing with argon2 or bcrypt (prefer argon2).

Add runtime validation to compare hashed keys.

Commit: fix(security): hash API keys at rest.

K.5 Prevent SQL injection (HIGH-005)

Replace all string-concatenated raw SQL with parameterized queries or query builder parameters.

Audit server/db.ts, server/routes.ts and add tests ensuring injection attempts are rejected.

Commit: fix(db): parametrize raw SQL queries.

K.6 Add rate limiting to health endpoint (HIGH-006)

Protect server/routes/health-enhanced.ts with a dedicated lightweight rate limiter (e.g., 10 requests/min per IP) and internal auth for staging.

Commit: fix(security): rate limit health checks.

K.7 CORS strictness (HIGH-008)

Implement origin validation in server/index.ts using ALLOWED_ORIGINS env var; disallow * in production.

Add automated test that simulates disallowed origin.

Commit: fix(security): enforce strict CORS in production.

K.8 Enforce Content-Type validation (HIGH-009)

Add middleware requireContentType.jsonOrMultipart that enforces application/json or multipart/form-data on POST/PUT/PATCH based on route.

Commit: fix(security): content-type validation middleware.

K.9 Log failed login attempts and set alerts (HIGH-010)

Aggregate failed logins in Redis counter per user/IP and expose a metric. Trigger an alert function if thresholds exceeded.

Add route/action to list suspicious IPs.

Commit: feat(security): aggregate failed logins and alerting hooks.

STEP L — PERFORMANCE & DATA INTEGRITY (HIGH)
L.1 Query caching (HIGH-011)

Add Redis caching layer for read-heavy endpoints in server/storage.ts with TTL and cache invalidation on write.

Add metrics for cache hit/miss.

Commit: perf(cache): add redis caching for read-heavy queries.

L.2 Fix N+1 queries (HIGH-012)

Audit endpoints and implement JOINs/batch loading or data loader pattern where applicable (orders, posts).

Add tests verifying number of DB queries per request is bounded.

Commit: perf(db): fix N+1 by batching & joins.

L.3 Pagination limits (HIGH-013)

Enforce server-side caps (max 100) and validate limit parameter; document in OpenAPI.

Commit: fix(api): enforce pagination caps.

L.4 Handle large file uploads without blocking (HIGH-014)

Replace disk-based synchronous multer storage for large files with streaming approach (busboy, multiparty) or direct S3 streaming; offload heavy processing to worker queue (BullMQ).

Commit: perf(upload): stream large file uploads and background processing.

L.5 Pool monitoring (HIGH-015)

Expose Prometheus metrics for DB pool and alert thresholds. Add a small dashboard snippet to observe pool metrics.

Commit: infra(metrics): add DB pool prometheus metrics.

L.6 Add foreign key cascade behavior (HIGH-016)

Update shared/schema.ts to include onDelete: 'cascade' or onDelete: 'set null' where appropriate; create migration.

Commit: fix(db): add foreign key cascade rules.

L.7 Fix order creation race (HIGH-017)

Wrap inventory decrement + order creation in DB transaction with SELECT ... FOR UPDATE or use optimistic locking with version numbers.

Add concurrency test simulating 100 concurrent order attempts.

Commit: fix(storage): transactional order creation to prevent oversell.

L.8 Validate updates with Zod (HIGH-018)

Add Zod schemas for all update routes and use drizzle-zod or middleware to validate partial updates.

Commit: fix(validation): validate updates with zod.

L.9 Soft delete & audit trail (HIGH-019 & HIGH-020)

Implement deletedAt column for soft-deletes and update all delete* methods to soft-delete.

Add audit_logs table and call auditUserAction() for password changes, 2FA disable, etc.

Commit: feat(audit): soft deletes and audit trails.

STEP M — TESTING (HIGH)
M.1 Integration tests for DB operations (HIGH-021)

Add integration test suite under server/__tests__/integration using a sandbox DB (e.g., test Neon DB or Docker Postgres) to cover transactions and race conditions (orders, sessions).

Commit: test(integration): add DB integration tests.

M.2 E2E tests for critical flows (HIGH-022)

Add Playwright tests for payment flow, 2FA flow, session expiry/refresh, WebSocket reconnection.

Commit: test(e2e): add critical path e2e tests.

M.3 Load testing configuration (HIGH-023)

Update k6/loadtest.js to include endpoints /api/products, /api/orders, /api/checkout with write load and an example script for 1000 concurrent virtual users (document how to run).

Commit: test(load): configure k6 load tests for key endpoints.

STEP N — MEDIUM & LOW PRIORITY IMPROVEMENTS
N.1 Refactor duplicate pagination logic, extract helpers (MED-001).
N.2 Replace magic numbers with shared/constants.ts and update code to use constants (MED-002).
N.3 Standardize error handling: implement ApiError and central error middleware (MED-003).
N.4 Sanitize search inputs (MED-004).
N.5 Remove commented-out code or document reason (MED-005).
N.6 Enforce camelCase naming and update files incrementally (MED-006).
N.7 Turn on TypeScript strict: true in tsconfig.json and fix resulting errors (MED-007).
N.8 Remove unused imports via eslint --fix (MED-008).
N.9 Add coverage to CI (vitest run --coverage) and ensure thresholds (MED-009).
N.10 Component splitting, accessibility, image optimization, PWA update prompt, offline queue persist, reconnection caps, environment separation, Docker-compose enhancements, healthcheck improvements, docs regeneration, architecture diagrams, runbook, CI Dependabot, canary/blue-green notes, and other MED/LOW items — implement iteratively with tests and documentation. Commit each logical grouping with descriptive messages.

FINAL VERIFICATION & CI

Run npm run ci:check and npm run ci:build. Fix all failures.

Run integration and e2e suites. Ensure no flaky tests; if flakiness arises, add retries and make tests deterministic.

Run npm audit and ensure no critical/ high vulnerabilities remain. Document any remaining moderate issues that require dependency changes or breaking upgrades.

Produce a release candidate tag remediation/full-p0-p1-p2-ready and push branch.

Create a PR with the branch, include the summary report, changed files, migration SQL, and required manual deployment notes (e.g., Redis setup, DB migrations, secret rotations).

Provide a rollback plan: how to revert migrations, disable new features, and restore circuit breaker state if needed.

OUTPUT REQUIREMENTS

For each commit: changed files list and brief rationale.

For each critical fix: before/after code snippets (diff summary), tests added, and test result output.

Migration scripts (SQL or drizzle migration files) to apply DB schema and index changes.

A single REMediation_SUMMARY.md at repo root with:

high-level changes

commands to run locally and in CI

env changes and .env.example

monitoring/metrics instructions

roll-back steps

CONSTRAINTS & SAFETY

Do not expose any secrets in commits or logs.

Use parameterized queries always.

For any change that may be breaking (API surface, DB schema), version bump and document migration steps.

If a migration is irreversible without downtime, clearly mark and include downtime window and step-by-step rollback.

END — run the remediation now and report progress inline as commits complete.